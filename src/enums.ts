export enum CookieKeys {
  PRIMARY_COLOR = 'myPisnickaColor',
  COLOR_SCHEME = 'myPisnickaScheme',
  FONT_SIZE = 'myPisnickaFontSize',
  SCROLL_SPEED = 'myPisnickaScrollSpeed',
}

export enum CookieAcceptState {
  ACCEPTED = "accepted",
  DECLINED = "declined",
  NONE = "none",
}